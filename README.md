More serializers for Zeek
=========================

This package implements two serializers for Zeek, in particular:

- `binary`: efficient binary representation
- `human`: JSON format

Users should treat the exact schema as opaque and must not rely on its exact format.
